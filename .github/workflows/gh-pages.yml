name: Deploy Static HTML to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write   # السماح بالكتابة على المستودع

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ جلب الكود
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1   # مهم لتقليل حجم git وتفادي المسارات الطويلة

      # 2️⃣ نشر الموقع
      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages        # الفرع الذي يُنشر عليه الموقع
          folder: .               # جذر المشروع (index.html موجود هنا)
          clean: true             # تنظيف الفرع قبل النشر
          clean-exclude: |        # استثناءات آمنة
            .gitignore
            README.md
